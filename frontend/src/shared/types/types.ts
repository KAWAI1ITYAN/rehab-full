export interface ReviewI {
  name: string,
  text: string,
  date: string
  id: string | number
}

export interface ReviewCreateI {
  name: string,
  text: string,
}

export interface bidI {
  name: string;
  phone: string;
}